import test from 'node:test'; import assert from 'node:assert/strict'; import { cardinalDirection } from '../src/utils.js'; import { findTides } from '../src/tides.js'; import { recommendWing } from '../src/wing.js'; import { addSession, normaliseSession, readSessions, todayISO } from '../src/sessions.js';
test('directions cardinales',()=>{assert.equal(cardinalDirection(0),'N');assert.equal(cardinalDirection(45),'NE');assert.equal(cardinalDirection(225),'SO');assert.equal(cardinalDirection(360),'N')});
test('détecte les extrema de marée',()=>{const result=findTides([{time:0,height:1},{time:1,height:3},{time:2,height:1},{time:3,height:2}]);assert.deepEqual(result,[{time:1,height:3,type:'high'},{time:2,height:1,type:'low'}])});
test('recommandation issue de la matrice',()=>{const r=recommendWing({weight:75,wind:20,level:'intermediate',board:'twin-tip',quiver:[7,9,12]});assert.equal(r.size,'9.0');assert.equal(r.owned,9)});
test('normalise et enregistre une session de kite',()=>{const data=new Map(),storage={getItem:key=>data.get(key)||null,setItem:(key,value)=>data.set(key,value)};const session=addSession({date:'2026-08-31',place:'Cabourg',averageWind:'18',wingSize:'9',boardSize:'138 cm',comments:'Belle session.'},storage);assert.deepEqual({...session,id:undefined},{id:undefined,date:'2026-08-31',place:'Cabourg',averageWind:18,wingSize:9,boardSize:'138 cm',comments:'Belle session.'});assert.equal(readSessions(storage).length,1)});
test('refuse une session incomplète et formate la date locale',()=>{assert.throws(()=>normaliseSession({date:'2026-08-31',place:'',averageWind:18,wingSize:9,boardSize:'138 cm'}),/Le lieu est obligatoire/);assert.equal(todayISO(new Date('2026-08-31T23:00:00-05:00')),'2026-09-01')});
